<% if logged_in? %>
  <%= form_for [@post, @comment] do |f| %>
    <%= f.text_area :content, :rows => 3 %><br>
    <%= f.submit %>
  <% end %>
<% else %>
  <%= link_to 'Sign in to comment', login_path, :class => 'btn btn-primary' %>
<% end %>